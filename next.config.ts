import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    // Add external domains used for images (update as needed for production)
    domains: [],
  },
};

export default nextConfig;
